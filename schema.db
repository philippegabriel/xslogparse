CREATE TABLE xensource (
	time timestamp,
	host varchar(20),
	pgm  varchar(20),
	level varchar(8),
	host2 varchar(20),
	domain varchar(100),
	taskname varchar(80),
	tasktype char,
	taskid char(12),
	process varchar(80),
	msg varchar(80)	
);
